-- =============== Modeling a Normalized Schema ===============
-- 1. Create a new postgres user named normal_user
-- CREATE USER normal_user;

-- 2. Create a new database named normal_cars owned by normal_user
-- CREATE DATABASE normal_cars OWNER normal_user;

-- 3. Whiteboard your solution to normalizing the denormal_cars schema.

-- [bonus] Generate a diagram (somehow) in .png (or other) format, that of your normalized cars schema. (save and commit to this repo)
